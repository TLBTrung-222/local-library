extends layout 

block content 
    h1 Author List
    ul 
        each author in authorList
            li 
                a(href=author.url)  #{author.name}  
                | (#{author.lifespan})

